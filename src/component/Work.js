
const Work = (props) => {
    return (
        <div id={props.contentId} className='bg-dark section'>
            <h2 className='text-white'>{props.title}</h2>            
        </div>
    );
}

export default Work;
